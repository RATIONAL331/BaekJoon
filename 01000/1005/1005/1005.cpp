#include<iostream>
#include<vector>
using namespace std;

int main() {
	int T;
	cin >> T;

	int N, K, W;
	while (T--) {
		cin >> N >> K;
		
		vector<int> cost(K);
		for (int i = 0; i < K; i++)
			cin >> cost[i];



		cin >> W;
	}
	return 0;
}